# PROCESSES — бизнес-процессы

Каждый процесс описывайте как цепочку в реальной работе: кто начинает, что к этому приводит (триггер), шаги по порядку, чем всё заканчивается при норме и что бывает при сбоях или исключениях. От этого потом строятся экраны и проверки.
